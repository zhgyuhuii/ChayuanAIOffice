/** Vitest stub: real shaped metrics load harfbuzz wasm; tests fall back to
 *  heuristics exactly like the not-initialized runtime path. */
export async function initShapedMetrics(): Promise<void> {}
export const shapedMeasure = undefined as never
export const shapedFamily = undefined as never
