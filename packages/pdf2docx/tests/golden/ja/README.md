# ja — Japanese golden cases

Target coverage: kana/kanji mixing without spaces, halfwidth katakana,
ruby-sized small runs (P1: plain small run). Vertical-text (tategaki) pages
must degrade to a full-page bitmap, never emit scrambled text.
