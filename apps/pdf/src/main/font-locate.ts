/**
 * System font location moved to @chatoffice/font-metrics (shared with the docs
 * metrics pipeline); re-exported here to keep pdf-main import paths stable.
 */
export { findFontCovering, findSystemFont, isTruetype } from '@chatoffice/font-metrics'
